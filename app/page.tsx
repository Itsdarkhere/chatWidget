import { ChatWidgetProvider } from "@/components/ChatContext";

export default function Home() {
  return (
    <ChatWidgetProvider>
      <main className="flex min-h-screen flex-col items-center justify-between p-24">
        
      </main>
    </ChatWidgetProvider>
  )
}
